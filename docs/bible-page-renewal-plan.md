# Bible 페이지 리뉴얼 기획서

> **작성일**: 2026-01-06
> **버전**: 1.0
> **상태**: 기획 완료, 구현 대기

---

## 1. 개요

### 1.1 목표
- 기존 `/reading` 페이지를 `/bible`로 완전 리뉴얼
- **통독모드**와 **성경읽기 모드** 분리
- 성경 앱 필수 기능 완성 (북마크, 묵상노트, 하이라이트 등)
- 컴포넌트 기반 아키텍처로 리팩토링

### 1.2 핵심 변경사항

| 기존 | 변경 후 | 설명 |
|------|--------|------|
| `/reading` | `/bible` | 성경 읽기 메인 |
| `/reading-plan` | `/plan` | 통독표 |
| 단일 페이지 | 분리된 라우트 | 북마크, 노트, 기록 별도 페이지 |

---

## 2. 라우트 구조

### 2.1 신규 라우트 맵

```
/bible                          → 성경 뷰어 메인 (진입점)
/bible?book=gen&chapter=1       → 읽기모드 (특정 장)
/bible?plan=1&schedule=5        → 통독모드

/bible/bookmarks                → 북마크 목록
/bible/notes                    → 묵상노트 목록
/bible/notes/[id]               → 노트 상세/편집 (전체화면)
/bible/history                  → 읽기 기록 및 통계
/bible/highlights               → 하이라이트 목록
/bible/settings                 → 읽기 설정

/plan                           → 통독표 메인
```

### 2.2 스마트 리다이렉트

기존 URL 호환성 유지:

| 기존 URL | 리다이렉트 |
|----------|-----------|
| `/reading?book=gen&chapter=1&plan=1` | `/bible?book=gen&chapter=1&plan=1` (통독모드) |
| `/reading?book=gen&chapter=1` | `/bible?book=gen&chapter=1` (읽기모드) |
| `/reading` | `/bible` |
| `/reading-plan` | `/plan` |

---

## 3. 모드 구분

### 3.1 읽기모드 (기본)

**진입 조건**: `plan` 쿼리 파라미터 없음

| 항목 | 동작 |
|------|------|
| 진입 경로 | `/bible`, `/bible?book=gen&chapter=1` |
| 진도 추적 | 개인 읽기 기록 (PersonalReadingRecord) |
| 완료 처리 | **수동 버튼** 클릭으로 "읽음" 표시 |
| 기능 | 북마크, 묵상노트, 하이라이트, 오디오 링크 |

### 3.2 통독모드

**진입 조건**: `plan` 쿼리 파라미터 존재

| 항목 | 동작 |
|------|------|
| 진입 경로 | `/plan`에서 일정 클릭 → 확인 모달 → `/bible?plan=x&schedule=y` |
| 진도 추적 | Plan 기반 진도 (UserBibleProgress) |
| 완료 처리 | 자동 + 수동 (아래 상세) |
| UI 표시 | 통독모드 인디케이터 (상단 배지) |

**통독모드 완료 처리:**
1. 마지막 장에서 "다음" 클릭 시 확인 모달 표시
2. 사용자 선택 옵션: "다음부터 자동으로 읽음 처리" 체크박스
3. 체크 시 이후 자동 완료 (설정에서 초기화 가능)
4. 조기 완료 버튼도 제공

---

## 4. 기능 명세

### 4.1 마지막 읽기 위치

**Backend**: ✅ 구현 완료 (`UserReadingPosition` 모델)

| 항목 | 설명 |
|------|------|
| 저장 정보 | 책, 장, 절, 스크롤 위치, 역본 |
| 저장 시점 | 장 변경 시 자동 저장 |
| 복원 시점 | `/bible` 진입 시 |

### 4.2 기본 진입점 동작

`/bible` 직접 접속 시 동작 (설정에서 변경 가능):

| 옵션 | 설명 | 기본값 |
|------|------|--------|
| 마지막 위치 | 저장된 마지막 읽던 위치로 이동 | ✅ 기본 |
| 홈/대시보드 | 최근 기록, 북마크, 노트 미리보기 | |
| 성경 목차 | 66권 목록에서 선택 | |

### 4.3 북마크

**Backend**: ✅ 구현 완료 (`BibleBookmark` 모델)

| 기능 | 설명 |
|------|------|
| 장 북마크 | "창세기 1장" 단위 |
| 절 북마크 | "요한복음 3:16-17" 범위 지정 |
| 메타데이터 | 제목, 색상, 메모 |
| 목록 페이지 | `/bible/bookmarks` |

### 4.4 묵상노트

**Backend**: ✅ 구현 완료 (`ReflectionNote` 모델)

| 기능 | 설명 |
|------|------|
| 작성 단위 | 장 또는 절 범위 |
| 간단 메모 | 모달에서 한 줄 메모 |
| 상세 편집 | `/bible/notes/[id]` 전체화면 에디터 |
| 비공개 설정 | 기본 비공개, 공개 전환 가능 |
| 목록 페이지 | `/bible/notes` |

### 4.5 하이라이트 (신규)

**Backend**: 구현 필요

| 기능 | 설명 |
|------|------|
| 색상 팔레트 | 기본 5색 (서비스 컬러 기반) |
| 사용자 지정 색 | 커스텀 색상 추가 가능 |
| 메모 첨부 | 하이라이트에 짧은 메모 |
| 목록 페이지 | `/bible/highlights` |

**제안 색상 팔레트:**
- `#FEF3C7` (노랑) - 기본
- `#D1FAE5` (초록)
- `#DBEAFE` (파랑)
- `#FEE2E2` (빨강)
- `#E9D5FF` (보라)

### 4.6 개인 읽기 기록

**Backend**: ✅ 구현 완료 (`PersonalReadingRecord` 모델)

| 기능 | 설명 |
|------|------|
| 기록 방식 | 수동 "읽음" 버튼 클릭 |
| 통계 | 전체 진도, 책별 진도, 연속 읽기 일수 |
| 목록 페이지 | `/bible/history` |

### 4.7 오디오 성경

**기존 방식 유지**: YouTube 링크 제공

| 항목 | 설명 |
|------|------|
| 데이터 소스 | `bible_chapter_videos.xlsx` |
| 연동 방식 | API `audio_link` 필드 |
| 모바일 | YouTube 앱 스키마 우선, 없으면 웹 |
| PC | 새 탭으로 열기 |

### 4.8 읽기 설정

`/bible/settings` 페이지 또는 설정 모달:

| 설정 항목 | 옵션 |
|----------|------|
| 폰트 크기 | 12px ~ 24px |
| 폰트 패밀리 | 시스템, 나눔명조, 나눔고딕 등 |
| 줄 간격 | 1.4 ~ 2.0 |
| 테마 | 라이트, 다크, 세피아 |
| 기본 진입점 | 마지막 위치 / 홈 / 목차 |
| 자동 읽음 처리 | On/Off (통독모드용) |

### 4.9 역본 비교

**1차 버전 제외** - 추후 구현 예정

---

## 5. UI/UX 상세

### 5.1 통독모드 진입 모달

```
┌─────────────────────────────────┐
│                                 │
│  📖 45주 성경통독               │
│                                 │
│  창세기 1-3장                   │
│                                 │
│  ┌─────────┐  ┌─────────────┐  │
│  │  취소   │  │  읽기 시작  │  │
│  └─────────┘  └─────────────┘  │
└─────────────────────────────────┘
```

### 5.2 통독모드 완료 확인 모달

```
┌─────────────────────────────────┐
│                                 │
│  📖 오늘 분량을 다 읽으셨나요?  │
│                                 │
│  창세기 1-3장                   │
│                                 │
│  ☐ 다음부터 자동으로 완료 처리  │
│    (설정 > 읽기 설정에서 변경)  │
│                                 │
│  ┌─────────┐  ┌─────────────┐  │
│  │  취소   │  │  완료 처리  │  │
│  └─────────┘  └─────────────┘  │
└─────────────────────────────────┘
```

### 5.3 통독모드 인디케이터

상단 헤더 아래 또는 플로팅 배지:

```
┌─────────────────────────────────┐
│ ⚡ 통독모드 | 창세기 1-3장  [X] │
└─────────────────────────────────┘
```

### 5.4 읽음 표시 버튼 (읽기모드)

하단 네비게이션 영역 또는 플로팅:

```
┌─────────────────────────────────┐
│  < 이전    창세기 1장    다음 > │
│                                 │
│       [ ✓ 읽음으로 표시 ]       │
└─────────────────────────────────┘
```

---

## 6. 기술 구현

### 6.1 파일 구조

```
frontend/app/
├── pages/
│   ├── bible/
│   │   ├── index.vue           # 성경 뷰어 메인
│   │   ├── bookmarks.vue       # 북마크 목록
│   │   ├── notes/
│   │   │   ├── index.vue       # 노트 목록
│   │   │   └── [id].vue        # 노트 상세/편집
│   │   ├── highlights.vue      # 하이라이트 목록
│   │   ├── history.vue         # 읽기 기록/통계
│   │   └── settings.vue        # 읽기 설정
│   ├── plan/
│   │   └── index.vue           # 통독표 (기존 reading-plan.vue 리팩토링)
│   ├── reading.vue             # 리다이렉트 전용 (호환성)
│   └── reading-plan.vue        # 리다이렉트 전용 (호환성)
│
├── components/
│   ├── bible/
│   │   ├── BibleViewer.vue     # 성경 본문 뷰어 (핵심)
│   │   ├── BibleHeader.vue     # 상단 헤더
│   │   ├── BibleNavigation.vue # 하단 네비게이션
│   │   ├── BookSelector.vue    # 책 선택 모달
│   │   ├── ChapterSelector.vue # 장 선택
│   │   ├── VersionSelector.vue # 역본 선택
│   │   ├── TongdokIndicator.vue # 통독모드 표시
│   │   ├── ReadingProgress.vue # 읽기 진도 표시
│   │   └── AudioButton.vue     # 오디오 링크 버튼
│   │
│   ├── bookmark/
│   │   ├── BookmarkButton.vue  # 북마크 토글 버튼
│   │   ├── BookmarkModal.vue   # 북마크 추가/편집 모달
│   │   └── BookmarkList.vue    # 북마크 목록 컴포넌트
│   │
│   ├── note/
│   │   ├── NoteButton.vue      # 노트 버튼 (카운트 표시)
│   │   ├── NoteQuickModal.vue  # 간단 메모 모달
│   │   ├── NoteEditor.vue      # 상세 에디터
│   │   └── NoteList.vue        # 노트 목록 컴포넌트
│   │
│   ├── highlight/
│   │   ├── HighlightPalette.vue # 색상 팔레트
│   │   ├── HighlightModal.vue   # 하이라이트 추가 모달
│   │   └── HighlightList.vue    # 하이라이트 목록
│   │
│   └── plan/
│       ├── TongdokEntryModal.vue  # 통독 진입 확인 모달
│       └── TongdokCompleteModal.vue # 통독 완료 확인 모달
│
├── composables/
│   ├── useBible.ts             # 성경 데이터 관련
│   ├── useBookmark.ts          # 북마크 CRUD
│   ├── useNote.ts              # 묵상노트 CRUD
│   ├── useHighlight.ts         # 하이라이트 CRUD (신규)
│   ├── useReadingRecord.ts     # 읽기 기록
│   ├── useReadingPosition.ts   # 마지막 위치
│   └── useTongdokMode.ts       # 통독모드 상태 관리
│
└── stores/
    ├── bible.ts                # 성경 상태 (현재 책/장/역본)
    └── readingSettings.ts      # 읽기 설정 (기존)
```

### 6.2 Backend 추가 작업

#### 6.2.1 Highlight 모델 (신규)

```python
class BibleHighlight(models.Model):
    """구절 하이라이트"""
    user = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.CASCADE,
        related_name='bible_highlights'
    )
    book = models.CharField(max_length=10)
    chapter = models.IntegerField()
    start_verse = models.IntegerField()
    end_verse = models.IntegerField()
    color = models.CharField(max_length=7, default='#FEF3C7')
    memo = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        verbose_name = '하이라이트'
        verbose_name_plural = '하이라이트'
        ordering = ['-created_at']
```

#### 6.2.2 신규 API 엔드포인트

| 엔드포인트 | 메서드 | 설명 |
|-----------|--------|------|
| `/api/v1/bible/highlights/` | GET | 하이라이트 목록 |
| `/api/v1/bible/highlights/` | POST | 하이라이트 생성 |
| `/api/v1/bible/highlights/<id>/` | PUT | 하이라이트 수정 |
| `/api/v1/bible/highlights/<id>/` | DELETE | 하이라이트 삭제 |
| `/api/v1/bible/highlights/by-chapter/` | GET | 장별 하이라이트 조회 |

### 6.3 리다이렉트 미들웨어

```typescript
// server/middleware/redirect-legacy-routes.ts
export default defineEventHandler((event) => {
  const url = getRequestURL(event);

  // /reading → /bible 리다이렉트
  if (url.pathname === '/reading') {
    const params = url.searchParams.toString();
    const redirectUrl = params ? `/bible?${params}` : '/bible';
    return sendRedirect(event, redirectUrl, 301);
  }

  // /reading-plan → /plan 리다이렉트
  if (url.pathname === '/reading-plan') {
    return sendRedirect(event, '/plan', 301);
  }
});
```

---

## 7. 구현 순서

### Phase 1: 기반 작업
1. 라우트 구조 설정 및 리다이렉트 구현
2. 기존 reading.vue를 컴포넌트로 분리
3. `/bible/index.vue` 기본 구조 구축

### Phase 2: 핵심 기능
4. 성경 뷰어 컴포넌트 완성 (BibleViewer)
5. 읽기모드 완성 (개인 기록, 읽음 버튼)
6. 통독모드 완성 (진입 모달, 완료 처리)
7. 마지막 읽기 위치 연동

### Phase 3: 부가 기능
8. 북마크 기능 완성 + 목록 페이지
9. 묵상노트 기능 완성 + 목록/상세 페이지
10. 하이라이트 기능 구현 (Backend + Frontend)

### Phase 4: 설정 및 마무리
11. 읽기 설정 페이지
12. 읽기 기록/통계 페이지
13. 기본 진입점 설정 기능
14. 테스트 및 버그 수정

---

## 8. 기존 문서 정리

이 문서로 대체되는 기존 문서:
- `docs/bible-reading-service-improvement-plan.md` → **삭제 예정**

기존 Backend 구현은 그대로 활용:
- UserReadingPosition 모델 ✅
- BibleBookmark 모델 ✅
- ReflectionNote 모델 ✅
- PersonalReadingRecord 모델 ✅
- 관련 API 엔드포인트 ✅

---

## 9. 미구현 사항 (추후 검토)

| 기능 | 사유 |
|------|------|
| 성경 전문 검색 | 서비스 구조상 어려움 |
| 역본 비교 | 1차 버전 범위 초과 |
| 공유 기능 (이미지) | 추후 검토 |

---

## 10. 체크리스트

### 기획 완료
- [x] 라우트 구조 확정
- [x] 모드 구분 (읽기/통독) 명세
- [x] 기능 목록 확정
- [x] UI/UX 흐름 정의
- [x] 파일 구조 설계
- [x] Backend 추가 작업 정의

### 구현 대기
- [ ] Phase 1: 기반 작업
- [ ] Phase 2: 핵심 기능
- [ ] Phase 3: 부가 기능
- [ ] Phase 4: 설정 및 마무리

---

> **다음 단계**: 구현 착수 시 이 문서를 참조하여 Phase 1부터 순차 진행
