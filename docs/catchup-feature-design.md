# 성경통독 따라잡기 기능 설계

> 작성일: 2026-01-03

## 개요

성경통독 플랜을 중도에 시작하거나 일정이 밀린 사용자를 위한 "따라잡기" 기능입니다.
Git의 fork 개념처럼, 원본 플랜을 유지하면서 개인화된 따라잡기 계획을 세울 수 있습니다.

### 핵심 목표

- **독려**: 밀렸다고 포기하지 않도록 현실적인 계획 제안
- **유연성**: 상황에 맞게 수정 가능한 구조
- **성취감**: 따라잡기 완료 시 축하로 동기부여

---

## 핵심 컨셉

```
원본 플랜: "2025 성경통독"
       ↓ (2주 밀림)
따라잡기 세션: "나의 1월 따라잡기 도전!" (사용자 지정 이름)
       ↓ (완료!)
🎉 축하 + 원본 플랜으로 복귀
```

### 부분 따라잡기 (메인 유즈케이스)

1년 플랜에서 2주 밀린 경우:
- ❌ 전체 플랜을 새로 만드는 것은 과도함
- ✅ 밀린 2주만 따라잡고 원본 플랜으로 복귀

---

## 따라잡기 전략

### 1. 동시 진행 (Parallel)

오늘 분량과 밀린 분량을 함께 읽습니다.

```
오늘 할 일:
- [원본] 마태복음 5장
- [따라잡기] 창세기 10장
- [따라잡기] 창세기 11장
```

**적합한 경우:**
- 순서 상관없이 읽을 수 있는 경우
- 빠르게 따라잡고 싶은 경우

### 2. 순차 복귀 (Sequential)

밀린 시점부터 순서대로 읽고, 목표일에 원본 플랜과 합류합니다.

```
1/15: 창세기 1-2장 (밀린 것)
1/16: 창세기 3-4장 (밀린 것)
...
1/28: 마태복음 4장 (원본 플랜과 합류!)
```

**적합한 경우:**
- 성경을 순서대로 읽어야 하는 경우
- 맥락을 유지하며 읽고 싶은 경우

---

## 데이터 모델

### CatchupSession (따라잡기 세션)

```python
class CatchupSession(models.Model):
    """따라잡기 세션"""

    STRATEGY_CHOICES = [
        ('parallel', '동시 진행'),
        ('sequential', '순차 복귀'),
    ]

    STATUS_CHOICES = [
        ('active', '진행 중'),
        ('completed', '완료'),
        ('abandoned', '포기'),
    ]

    # 기본 정보
    subscription = models.ForeignKey(
        PlanSubscription,
        on_delete=models.CASCADE,
        related_name='catchup_sessions'
    )
    name = models.CharField(
        max_length=100,
        help_text="사용자 지정 이름 (예: 나의 1월 도전!)"
    )

    # 따라잡기 범위
    range_start = models.DateField(help_text="밀린 기간 시작일")
    range_end = models.DateField(help_text="밀린 기간 종료일")

    # 전략 및 목표
    strategy = models.CharField(
        max_length=20,
        choices=STRATEGY_CHOICES,
        default='parallel'
    )
    target_rejoin_date = models.DateField(
        null=True,
        blank=True,
        help_text="순차 복귀 모드에서 원본 플랜 합류 목표일"
    )

    # 읽기량 설정
    max_daily_readings = models.IntegerField(
        null=True,
        blank=True,
        help_text="하루 최대 읽기 횟수"
    )
    max_daily_chapters = models.IntegerField(
        null=True,
        blank=True,
        help_text="하루 최대 읽기 장 수"
    )
    weekend_multiplier = models.DecimalField(
        max_digits=3,
        decimal_places=1,
        default=1.0,
        help_text="주말 읽기량 배수 (예: 1.5 = 평일의 1.5배)"
    )

    # 상태
    status = models.CharField(
        max_length=20,
        choices=STATUS_CHOICES,
        default='active'
    )
    completed_at = models.DateTimeField(null=True, blank=True)

    # 타임스탬프
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        ordering = ['-created_at']

    def __str__(self):
        return f"{self.name} ({self.subscription.plan.name})"

    @property
    def progress_percentage(self):
        """진행률 계산"""
        total = self.schedules.count()
        if total == 0:
            return 0
        completed = self.schedules.filter(is_completed=True).count()
        return int((completed / total) * 100)

    @property
    def remaining_count(self):
        """남은 스케줄 수"""
        return self.schedules.filter(is_completed=False).count()
```

### CatchupSchedule (따라잡기 스케줄)

```python
class CatchupSchedule(models.Model):
    """따라잡기 세션의 개별 스케줄"""

    session = models.ForeignKey(
        CatchupSession,
        on_delete=models.CASCADE,
        related_name='schedules'
    )
    original_schedule = models.ForeignKey(
        DailyBibleSchedule,
        on_delete=models.CASCADE,
        help_text="원본 스케줄 참조"
    )
    scheduled_date = models.DateField(
        help_text="새로 배정된 날짜"
    )

    # 완료 상태
    is_completed = models.BooleanField(default=False)
    completed_at = models.DateTimeField(null=True, blank=True)

    # 타임스탬프
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        ordering = ['scheduled_date', 'original_schedule__date']
        unique_together = ['session', 'original_schedule']

    def __str__(self):
        return f"{self.session.name} - {self.original_schedule}"

    def mark_as_completed(self):
        self.is_completed = True
        self.completed_at = timezone.now()
        self.save()

    def mark_as_incomplete(self):
        self.is_completed = False
        self.completed_at = None
        self.save()
```

---

## API 엔드포인트

### 따라잡기 현황 조회

```
GET /api/v1/todos/subscriptions/{subscription_id}/catchup-status/
```

**Response:**
```json
{
  "has_overdue": true,
  "overdue_count": 14,
  "overdue_range": {
    "start": "2025-01-01",
    "end": "2025-01-14"
  },
  "active_catchup_session": null,
  "suggested_settings": {
    "max_daily_readings": 3,
    "estimated_days": 7,
    "estimated_rejoin_date": "2025-01-21"
  }
}
```

### 따라잡기 미리보기

```
POST /api/v1/todos/subscriptions/{subscription_id}/catchup/preview/
```

**Request:**
```json
{
  "range_start": "2025-01-01",
  "range_end": "2025-01-14",
  "strategy": "sequential",
  "max_daily_readings": 3,
  "target_rejoin_date": "2025-01-28"
}
```

**Response:**
```json
{
  "valid": true,
  "summary": {
    "total_schedules": 14,
    "daily_average": 2.5,
    "estimated_days": 6,
    "rejoin_date": "2025-01-28"
  },
  "preview_schedules": [
    {
      "date": "2025-01-15",
      "items": [
        {"book": "창세기", "chapters": "1-2"},
        {"book": "창세기", "chapters": "3-4"}
      ]
    }
  ],
  "warnings": []
}
```

### 따라잡기 세션 생성

```
POST /api/v1/todos/subscriptions/{subscription_id}/catchup/
```

**Request:**
```json
{
  "name": "나의 1월 도전!",
  "range_start": "2025-01-01",
  "range_end": "2025-01-14",
  "strategy": "sequential",
  "max_daily_readings": 3,
  "max_daily_chapters": null,
  "weekend_multiplier": 1.5,
  "target_rejoin_date": "2025-01-28"
}
```

### 따라잡기 세션 수정

```
PATCH /api/v1/todos/catchup-sessions/{session_id}/
```

설정 수정 후 스케줄 재계산이 필요한 경우 `recalculate: true` 포함.

### 따라잡기 스케줄 완료

```
POST /api/v1/todos/catchup-schedules/{schedule_id}/complete/
```

### 따라잡기 세션 완료

```
POST /api/v1/todos/catchup-sessions/{session_id}/complete/
```

**Response:**
```json
{
  "success": true,
  "message": "축하합니다! '나의 1월 도전!'을 완료했습니다!",
  "stats": {
    "total_completed": 14,
    "days_taken": 6,
    "started_at": "2025-01-15",
    "completed_at": "2025-01-20"
  }
}
```

---

## UX 흐름

### 1. 진입점

**캘린더/메인 화면:**
- 밀린 일정 알림 배지 표시
- "따라잡기 시작" 버튼

**플랜 상세 페이지:**
- 밀린 현황 요약
- "따라잡기 계획 세우기" 버튼

### 2. 설정 화면

```
┌─────────────────────────────────────────┐
│  📚 따라잡기 계획 세우기                   │
├─────────────────────────────────────────┤
│                                         │
│  📝 이름                                 │
│  ┌─────────────────────────────────┐    │
│  │ 나의 1월 도전!                    │    │
│  └─────────────────────────────────┘    │
│                                         │
│  📅 따라잡을 범위                         │
│  ○ 오늘까지 밀린 것 전부 (14일치)          │
│  ● 기간 직접 선택                         │
│    1월 1일 ~ 1월 14일                    │
│                                         │
│  🎯 진행 방식                             │
│  ○ 동시 진행 - 오늘 분량과 함께 읽기        │
│  ● 순차 복귀 - 밀린 것부터 순서대로         │
│                                         │
│  📖 하루 최대 읽기량                       │
│  ┌────┐ 회  또는  ┌────┐ 장             │
│  │ 3  │          │    │               │
│  └────┘          └────┘               │
│                                         │
│  📆 주말에 더 읽기                         │
│  ☑ 주말에는 평일의 1.5배                  │
│                                         │
│  🏁 목표 복귀일                           │
│  💡 추천: 1월 25일 (하루 3회 기준)         │
│  ┌─────────────────────────────────┐    │
│  │ 1월 28일                         │    │
│  └─────────────────────────────────┘    │
│                                         │
│  ┌─────────────┐  ┌─────────────────┐   │
│  │   미리보기   │  │  계획 시작하기   │   │
│  └─────────────┘  └─────────────────┘   │
└─────────────────────────────────────────┘
```

### 3. 미리보기 화면

```
┌─────────────────────────────────────────┐
│  📊 미리보기                              │
├─────────────────────────────────────────┤
│                                         │
│  이대로 진행하면...                        │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │ 📚 총 14일치 따라잡기              │    │
│  │ 📈 하루 평균 2.5회 읽기            │    │
│  │ 📅 1월 28일에 원본 플랜과 합류      │    │
│  │ ⏱️ 예상 소요: 6일                  │    │
│  └─────────────────────────────────┘    │
│                                         │
│  일자별 계획                              │
│  ┌─────────────────────────────────┐    │
│  │ 1/15 (수) - 창세기 1-2장, 3-4장    │    │
│  │ 1/16 (목) - 창세기 5-6장, 7-8장    │    │
│  │ 1/17 (금) - 창세기 9-10장, 11장    │    │
│  │ 1/18 (토) - 창세기 12-14장 ⭐      │    │
│  │ ...                              │    │
│  └─────────────────────────────────┘    │
│                                         │
│  ┌─────────────┐  ┌─────────────────┐   │
│  │   다시 설정  │  │   시작하기 🚀   │   │
│  └─────────────┘  └─────────────────┘   │
└─────────────────────────────────────────┘
```

### 4. 진행 중 화면

메인 화면에서 플랜 표시 방식:

```
┌─────────────────────────────────────────┐
│  📖 오늘의 읽기                           │
├─────────────────────────────────────────┤
│                                         │
│  ~~2025 성경통독~~ (취소선)               │
│  ↓                                      │
│  🏃 나의 1월 도전!                        │
│                                         │
│  진행률: ████████░░ 80% (11/14)          │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │ ☑ 창세기 10장                     │    │
│  │ ☐ 창세기 11장                     │    │
│  │ ☐ 창세기 12장                     │    │
│  └─────────────────────────────────┘    │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │ ⚙️ 계획 수정  │  ❌ 따라잡기 종료  │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

### 5. 완료 화면

```
┌─────────────────────────────────────────┐
│                                         │
│              🎉🎊🎉                      │
│                                         │
│         축하합니다!                       │
│                                         │
│    "나의 1월 도전!" 완료!                 │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │ 📚 14일치 읽기 완료!              │    │
│  │ ⏱️ 6일 만에 달성!                 │    │
│  │ 🏆 대단해요!                      │    │
│  └─────────────────────────────────┘    │
│                                         │
│    이제 원본 플랜으로 돌아갑니다.           │
│    "2025 성경통독"                       │
│                                         │
│         ┌─────────────────┐             │
│         │      확인       │             │
│         └─────────────────┘             │
└─────────────────────────────────────────┘
```

### 6. 수정 (힘들 때 / 더 할 수 있을 때)

언제든 설정 변경 가능:
- 하루 읽기량 조절
- 목표 복귀일 변경
- 따라잡기 범위 재설정
- 새로운 따라잡기 세션으로 재시작

---

## 알고리즘

### 스케줄 분배 알고리즘

```python
def calculate_catchup_schedule(
    overdue_schedules: List[DailyBibleSchedule],
    start_date: date,
    target_date: date,
    max_daily_readings: int = None,
    max_daily_chapters: int = None,
    weekend_multiplier: float = 1.0,
    strategy: str = 'sequential'
) -> List[CatchupScheduleData]:
    """
    밀린 스케줄을 새 날짜에 분배

    Args:
        overdue_schedules: 밀린 스케줄 목록
        start_date: 따라잡기 시작일
        target_date: 목표 복귀일
        max_daily_readings: 하루 최대 읽기 횟수
        max_daily_chapters: 하루 최대 장 수
        weekend_multiplier: 주말 배수
        strategy: 'sequential' 또는 'parallel'

    Returns:
        새로 배정된 스케줄 목록
    """
    result = []
    remaining = list(overdue_schedules)
    current_date = start_date

    while remaining and current_date <= target_date:
        # 주말 여부 확인
        is_weekend = current_date.weekday() >= 5
        daily_limit = max_daily_readings
        if is_weekend and daily_limit:
            daily_limit = int(daily_limit * weekend_multiplier)

        # 오늘 배정할 스케줄
        today_schedules = []
        today_chapters = 0

        while remaining:
            schedule = remaining[0]
            chapters = schedule.end_chapter - schedule.start_chapter + 1

            # 제한 확인
            if daily_limit and len(today_schedules) >= daily_limit:
                break
            if max_daily_chapters and today_chapters + chapters > max_daily_chapters:
                break

            today_schedules.append(schedule)
            today_chapters += chapters
            remaining.pop(0)

        # 결과에 추가
        for schedule in today_schedules:
            result.append(CatchupScheduleData(
                original_schedule=schedule,
                scheduled_date=current_date
            ))

        current_date += timedelta(days=1)

    return result
```

---

## 구현 우선순위

### Phase 1: 핵심 기능

1. 모델 생성 (`CatchupSession`, `CatchupSchedule`)
2. 밀린 현황 조회 API
3. 따라잡기 미리보기 API
4. 따라잡기 세션 생성 API
5. 스케줄 완료 처리 API

### Phase 2: 프론트엔드

1. 설정 모달/페이지
2. 미리보기 화면
3. 진행 중 표시 (메인 화면 통합)
4. 완료 축하 화면

### Phase 3: 고도화

1. 설정 수정 및 재계산
2. 알림/리마인더
3. 통계/성취 배지
4. 주말 배수 등 고급 설정

---

## 참고 사항

### 원본 플랜과의 관계

- 원본 플랜 구독은 **유지**됨 (비활성화하지 않음)
- 따라잡기 세션은 원본 구독에 **연결**됨
- 완료 후 자연스럽게 원본 플랜으로 **복귀**

### 유연성

- 따라잡기 중 추가로 밀려도 **재설정 가능**
- "너무 힘들다" → 읽기량 줄이기, 복귀일 연장
- "더 할 수 있다" → 읽기량 늘리기, 복귀일 앞당기기
- 언제든 포기하고 **새 세션** 시작 가능

### 동기부여

- 진행률 시각화
- 완료 시 축하 메시지
- 사용자 지정 이름으로 개인화
