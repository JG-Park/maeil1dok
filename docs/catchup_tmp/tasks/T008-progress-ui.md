# T008: 진행 중 표시 UI

> 상태: `pending`
> 의존: T006, T007
> 커밋: -

## 목표

메인 화면(캘린더/읽기 화면)에서 따라잡기 진행 상황을 표시하고 관리하는 UI를 구현합니다.

## UI 설계

### 메인 화면 (따라잡기 활성 시)

```
┌─────────────────────────────────────────┐
│  📖 오늘의 읽기                          │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 🏃 나의 1월 도전!                 │   │
│  │                                  │   │
│  │ ████████░░░░░░░░ 57% (8/14)      │   │
│  │                                  │   │
│  │ 원본: ~~2025 성경통독~~          │   │
│  └─────────────────────────────────┘   │
│                                         │
│  📅 오늘 할 읽기 (1/17)                  │
│  ┌─────────────────────────────────┐   │
│  │ [따라잡기]                       │   │
│  │ ☑ 창세기 5-6장                   │   │
│  │ ☐ 창세기 7-8장                   │   │
│  │ ☐ 창세기 9장                     │   │
│  ├─────────────────────────────────┤   │
│  │ [원본 플랜] - 동시 진행 모드      │   │
│  │ ☐ 마태복음 5장                   │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────┐  ┌─────────────────┐  │
│  │  ⚙️ 계획수정  │  │  ✅ 따라잡기완료 │  │
│  └─────────────┘  └─────────────────┘  │
└─────────────────────────────────────────┘
```

### 순차 복귀 모드 (원본 일시 중단)

```
┌─────────────────────────────────────────┐
│  📖 오늘의 읽기                          │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 🏃 나의 1월 도전!                 │   │
│  │                                  │   │
│  │ ████████░░░░░░░░ 57% (8/14)      │   │
│  │                                  │   │
│  │ 📅 1/28에 원본 플랜과 합류 예정    │   │
│  └─────────────────────────────────┘   │
│                                         │
│  📅 오늘 할 읽기 (1/17)                  │
│  ┌─────────────────────────────────┐   │
│  │ ☑ 창세기 5-6장                   │   │
│  │ ☐ 창세기 7-8장                   │   │
│  │ ☐ 창세기 9장                     │   │
│  └─────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘
```

## 작업 내용

### 1. 진행 상황 카드

`frontend/app/components/catchup/CatchupProgressCard.vue`:

```vue
<script setup lang="ts">
interface CatchupSession {
  id: number
  name: string
  status: 'active' | 'completed' | 'abandoned'
  strategy: 'parallel' | 'sequential'
  target_rejoin_date?: string
  progress_percentage: number
  total_schedules: number
  completed_schedules: number
  subscription: {
    plan: {
      name: string
    }
  }
}

const props = defineProps<{
  session: CatchupSession
}>()

const emit = defineEmits(['edit', 'complete'])
</script>

<template>
  <div class="catchup-progress-card">
    <div class="header">
      <span class="icon">🏃</span>
      <h3>{{ session.name }}</h3>
    </div>

    <div class="progress-bar">
      <div
        class="progress-fill"
        :style="{ width: `${session.progress_percentage}%` }"
      />
    </div>
    <div class="progress-text">
      {{ session.progress_percentage }}%
      ({{ session.completed_schedules }}/{{ session.total_schedules }})
    </div>

    <div class="original-plan">
      <template v-if="session.strategy === 'parallel'">
        원본: <s>{{ session.subscription.plan.name }}</s>
      </template>
      <template v-else>
        📅 {{ formatDate(session.target_rejoin_date) }}에 원본 플랜과 합류 예정
      </template>
    </div>

    <div class="actions">
      <button @click="emit('edit')" class="btn-secondary">
        ⚙️ 계획수정
      </button>
      <button @click="emit('complete')" class="btn-primary">
        ✅ 따라잡기완료
      </button>
    </div>
  </div>
</template>
```

### 2. 오늘의 읽기 목록 수정

`frontend/app/components/reading/TodayReadingList.vue` 수정:

- 따라잡기 세션이 활성인 경우:
  - 따라잡기 스케줄 먼저 표시
  - 동시 진행 모드면 원본 스케줄도 함께 표시
  - 순차 복귀 모드면 따라잡기만 표시

### 3. API 연동

```typescript
// 오늘의 따라잡기 스케줄 조회
const fetchTodayCatchupSchedules = async (sessionId: number) => {
  const today = new Date().toISOString().split('T')[0]
  return await $fetch(
    `/api/v1/todos/catchup-sessions/${sessionId}/schedules/?date=${today}`
  )
}

// 스케줄 완료 토글
const toggleCatchupSchedule = async (scheduleId: number) => {
  return await $fetch(
    `/api/v1/todos/catchup-schedules/${scheduleId}/toggle/`,
    { method: 'POST' }
  )
}
```

### 4. 상태 관리

`frontend/app/stores/catchup.ts`:

```typescript
export const useCatchupStore = defineStore('catchup', () => {
  const activeSession = ref<CatchupSession | null>(null)
  const todaySchedules = ref<CatchupSchedule[]>([])

  const hasActiveSession = computed(() => !!activeSession.value)

  const fetchActiveSession = async (subscriptionId: number) => {
    // 활성 세션 조회
  }

  const fetchTodaySchedules = async () => {
    // 오늘 스케줄 조회
  }

  const toggleSchedule = async (scheduleId: number) => {
    // 토글 및 상태 업데이트
  }

  return {
    activeSession,
    todaySchedules,
    hasActiveSession,
    fetchActiveSession,
    fetchTodaySchedules,
    toggleSchedule
  }
})
```

## 파일 변경

- `frontend/app/components/catchup/CatchupProgressCard.vue` (신규)
- `frontend/app/components/catchup/TodayCatchupList.vue` (신규)
- `frontend/app/stores/catchup.ts` (신규)
- `frontend/app/pages/reading.vue` (수정)
- `frontend/app/components/reading/TodayReadingList.vue` (수정)

## 검증 방법

1. 따라잡기 세션 생성 후 메인 화면 확인
2. 진행 상황 카드 표시 확인
3. 오늘의 따라잡기 스케줄 표시
4. 체크박스 토글 시 진행률 업데이트
5. 동시 진행 모드: 원본 + 따라잡기 함께 표시
6. 순차 복귀 모드: 따라잡기만 표시

## 완료 조건

- [ ] 진행 상황 카드 표시 (이름, 진행률)
- [ ] 원본 플랜 취소선 표시
- [ ] 오늘의 따라잡기 스케줄 표시
- [ ] 체크박스 토글 동작
- [ ] 진행률 실시간 업데이트
- [ ] 동시 진행 모드 UI
- [ ] 순차 복귀 모드 UI
- [ ] 계획수정 버튼 → 설정 모달
- [ ] 따라잡기완료 버튼 → T009 화면
